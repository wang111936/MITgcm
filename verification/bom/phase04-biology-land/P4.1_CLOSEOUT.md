# MITGCM-BOM P4.1 local closeout

Status: **LOCALLY COMPLETE; NOT PUSHED OR INTEGRATED**

P4.1 implements only the frozen parameter, accepted biology-field and
stateless Brooks increment.  It does not enter the live owner/event path and
does not implement boundary transitions, slot reuse, births, terminal-event
commit, event I/O or schema 4.

## Accepted scope

- inactive-by-default biology/land parameters and appended stable failure,
  event and P4 phase codes;
- guarded THETA/PTRACER and THETA/FILES configuration validation, including
  one conversion of daily growth/mortality rates to SI seconds;
- transactional accepted surface temperature/nutrient OLD/NEW endpoints,
  shared wet validity, exact labels, rollback and clean retry;
- exact/interior time interpolation followed by common normalized wet-space
  weights and explicit STOP/NO_GROWTH missing-sample policy;
- finite stateless Brooks factors and an immutable amount/event proposal;
- fail-closed rejection of live biology owners, restart and output until the
  later event/schema packages exist.

## Exact-head evidence

The accepted 40-character source identity is stored in `source-head.txt` in
each evidence root and must equal the commit containing this closeout record.

1. P4.1 direct gate: 31/31 PASS
   - `/home/wyl/projects/mitgcm-bom-test-artifacts/phase04/p41/p41-final-head-attempt02`
   - serial, MPI4 and no-PTRACERS debug/IEEE builds;
   - PTRACER and FILES endpoint transactions, rollback/retry and decomposition;
   - common-wet exact-time interpolation and missing policy;
   - 13 Brooks rows against the independent 90-digit Decimal oracle;
   - analytical amount plans, strict thresholds and 11 fail-before-state runs.
2. Exact predecessor replay: 538/538 PASS
   - `/home/wyl/projects/mitgcm-bom-test-artifacts/phase03/p3-g99/p41-predecessor-final-head-attempt02`
   - P3.1--P3.5 direct groups: 148/148;
   - Phase 2 closure, including FLT+BOM coexistence: 390/390;
   - replay mode fixes the audit baseline at released v0.4 while leaving the
     released candidate/final modes unchanged.

Both gates require a clean exact source, immutable row names, source/driver/
input/reference hashes and a self-validating manifest.  Evidence is external
to Git and is not a substitute for later P4.2--P4.5 gates.

## Findings resolved during implementation

- Fortran logical expressions were split where non-short-circuit evaluation
  could compare NaN values or evaluate an overflowing guard expression.
- Brooks difference/ratio and FILES time/scale arithmetic use the existing
  checked add/multiply helpers.
- Periodic endpoint halo aliases are validated by production exchange while
  unique interior values provide unambiguous fixture expectations.
- Negative MITgcm runs are recognized by fatal markers and absence of normal
  completion because a compiler `STOP` can return shell status zero.
- A P4.1 predecessor replay scope was added to the P2/P3 independent audits;
  it compares against the exact v0.4 release commit and does not weaken the
  original P2.5/P3.1/P3.3/P3.5 or P3 candidate/final scopes.

## Exit decision

P4-R02--P4-R05 are locally verified.  The focused P4-R01/P4-R19 obligations
for disabled-path compatibility and serial/MPI endpoint/law equivalence are
also verified.  P4.1 is ready for a later authorized push and Draft PR.

The unique next production package is P4.2: boundary classification, RK
terminal scratch, terminal-state planning/rollback and compact-tail free-slot
management.  No tag is created at P4.1.

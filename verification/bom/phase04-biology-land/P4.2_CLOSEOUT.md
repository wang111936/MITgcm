# MITGCM-BOM P4.2 local closeout

Status: **LOCALLY COMPLETE; NOT PUSHED OR INTEGRATED**

P4.2 implements only the frozen boundary, terminal-event and compact-tail
increment. It does not implement births, Philox, global child IDs, complete
biology-aware migration, event files, schema 4 persistence or restart.

## Accepted scope

- finite regular-grid WET/BEACHED/OUTSIDE/FATAL classification, including
  periodic-X normalization, half-open faces, exact owner selection, wet
  support and unsupported-grid rejection;
- additive RK2/RK4 P4 interfaces that retain the last accepted wet position,
  first terminal attempt and first encounter stage while preserving the
  released interfaces for predecessor callers;
- synchronous spring exclusion and deferred exact-ID binding across owner
  migration;
- all-or-nothing post-migration boundary/death transaction with canonical
  first failure, bounded in-memory events, counter/live-budget preflight and
  complete rollback before the first authoritative write;
- compact owner prefixes, deterministic derived free tails, O(1) tail-swap
  deletion/allocation and rebuild after initial read, migration and pickup;
- land-only schema-2 migration maintenance for initial parent/count/S
  defaults without widening the released packet. Complete parent/S/birth
  migration remains P4.3 packet schema 3 scope;
- WAITING owners remain frozen and do not sample biology fields.

## Exact-head evidence

Every accepted root stores the same 40-character source identity in
`source-head.txt`; it must equal the commit containing this closeout record.

1. P4.2 direct gate: 18/18 PASS
   - `/home/wyl/projects/mitgcm-bom-test-artifacts/phase04/p42/p42-final-head-attempt03`
   - serial and MPI4 debug/IEEE builds;
   - P4-L01 wet/dry/outside/periodic/fatal classifier matrix;
   - P4-F01 empty/partial/full compact tails, O(1) removal/allocation,
     migration and deterministic reconstruction;
   - P4-T01 six injected rollback phases with owner/free/ID/event/graph/
     endpoint/schedule sentinels;
   - RK2/RK4 K1--K4/FINAL land/outside encounters, synchronous spring,
     distinct B11 terminal records and interleaved B13 deaths/reuse.
2. Accepted P4.1 direct gate: 31/31 PASS
   - `/home/wyl/projects/mitgcm-bom-test-artifacts/phase04/p41/p42-p41-accepted-attempt04`
   - replays the complete endpoint/interpolation/Brooks predecessor on the
     same clean P4.2 head.
3. Exact v0.4 predecessor replay: 538/538 PASS
   - `/home/wyl/projects/mitgcm-bom-test-artifacts/phase03/p3-g99/p42-predecessor-final-head-attempt04`
   - P3.1--P3.5 direct groups: 148/148;
   - Phase 2 closure, including the compatibility-only FLT+BOM coexistence
     regression: 390/390; BOM remains independent and has no FLT runtime
     dependency;
   - executed in an isolated shared clone with its local v0.4 tag removed;
     the authoritative repository branches and annotated tag are unchanged.

The gates require a clean exact source, fixed row names, source/driver/input
hashes and self-validating manifests. Generated evidence remains outside Git.

## Findings resolved during implementation

- periodic normalization now handles negative non-zero origins before owner
  classification;
- absent Stokes/wind providers remain exact zero sources and are not mistaken
  for invalid non-zero endpoint data;
- the P4 RK interfaces are additive, so all released Phase 2 callers retain
  their original argument lists;
- every free-tail/counter/capacity condition and all global reductions occur
  before event commit;
- the P3-G99 predecessor audit recognizes only the fixed nine-integer
  per-rank first-failure gather; released candidate/final gather rules remain
  unchanged and no live owner is gathered;
- migration scratch binds by exact ID after owner exchange, while owner P4
  tail fields are cleared so post-migration free reconstruction is valid;
- production live biology remains fail-closed until P4.3 supplies complete
  schema-3 migration, birth ordering and graph integration.

## Exit decision

P4-R06, P4-R07 and P4-R10 are locally verified. The P4.2 transaction and
terminal-buffer portions of P4-R08/P4-R09, plus focused P4-R01/P4-R19
compatibility/decomposition obligations, are also verified. Later schema,
graph, birth, persistence and final-budget obligations remain open.

The unique next production package is P4.3: Philox/retry, global parent order
and child IDs, complete packet schema 3, and post-event graph integration.
No tag is created at P4.2.

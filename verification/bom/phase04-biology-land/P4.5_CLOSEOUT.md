# MITGCM-BOM P4.5 closeout

Status: **INTEGRATED; RELEASE-CANDIDATE P4-G99 AND EXIT AUDIT PASS**

P4.5 closes only the frozen Phase 4 capacity matrix and supplies the final
P4-G99/independent-exit drivers. It does not implement Phase 5 scaling or
Phase 6 grid support.

## 1. Production increment

- `BOM_EVENT_PREFLIGHT` rejects global live, destination-tile, event
  metadata/allgather, unflushed-buffer and signed exact-ID overflow before
  any authoritative mutation;
- `BOM_OWNER_EXCHANGE_PREFLIGHT` rejects send, receive and destination-tile
  packet overflow before owner replacement;
- birth-order capacity failure no longer enters an oversized `MPI_Allgatherv`
  and cannot be overwritten by a later ordinal/ID diagnostic;
- post-birth candidate, neighbor and ghost overflow converge through the
  common global status reduction before any rank enters component
  collectives;
- all failures retain the stable code/phase, need/capacity and canonical
  first-failure context and preserve bitwise transaction rollback.

## 2. B19 evidence

The frozen compact-limit driver covers exactly 19 rows. Exact package-head
evidence `p45-final-head-attempt11` passed 19/19 in serial/MPI paths, including:

- exact-equality success and one-above failure for global, tile, event,
  buffer, exchange and ID limits;
- actual parent-order equality/overflow without an unsafe collective;
- candidate 4/4 versus 4/3, neighbor 3/3 versus 3/2 and ghost 4/4 versus
  5/4 limits after births;
- actual event-transaction neighbor overflow at stable code 20, P4 graph
  phase 8, canonical rank/context zero and bitwise owner rollback.

Evidence root:
`/home/wyl/projects/mitgcm-bom-test-artifacts/phase04/p45/p45-final-head-attempt11`.

## 3. Final closure contract

After this package is merged to `MITGCM-BOM/development`, P4-G99 must run on
the clean exact merge head and register 31 P4.1, 18 P4.2, 26 P4.3, 57 P4.4,
19 P4.5 and 538 exact predecessor rows: 689/689 total. The independent exit
audit must then verify P4-R01--P4-R20, ordered package ancestry, frozen schema,
bounded event metadata, no live-owner event gather and no Phase 5/6
completion claim.

P4.5 PR #38 was merged as
`3c066e7d86bf6007411c6d0893ffd4e8db81dcf9`. Integrated P4-G99 then exposed
five historical audit/isolated-clone compatibility defects; PR #39--#43 fixed
them without changing the frozen production semantics.

The clean release-candidate head
`9a468ec3d642986f292b941aa6612d74301dda91` passed P4-G99 689/689 at
`p4-g99-release-candidate-9a468ec3d-attempt06` and the independent exit audit
at `phase4-exit-release-candidate-9a468ec3d-attempt01`. The release tag remains
forbidden until the exit-record PR is merged and both gates pass again on the
final release head. See [Phase 4 exit audit](PHASE4_EXIT_AUDIT.md).

# P1.1 scope audit

Status: **PASS**

## Allowed production scope

- `pkg/bom/BOM.h`
- `pkg/bom/BOM_SIZE.h`
- `pkg/bom/bom_readparms.F`
- `pkg/bom/bom_check.F`
- `pkg/bom/bom_init_varia.F`
- new P1.1-only `pkg/bom/bom_*` state, ID, initial locator, and reader routines

Allowed supporting scope is limited to `verification/bom/phase01-bom-lite`, `verification/bom/README.md`, and Phase 1 design/status records under `doc/phys_pkgs/MITGCM-BOM`.

## Explicit exclusions verified

- no `model/src` or `model/inc` lifecycle change;
- no `pkg/flt` change or runtime call into FLT;
- no Phase 0 reference, input, script, or compact evidence change;
- no environmental-field array, C-grid rotation, halo exchange, or interpolation;
- no `BOM_MAIN` motion, RK method, owner migration, trajectory, or pickup;
- no Stokes field allocation or read path;
- no build tree, executable, runtime output, Python cache, or generated MDS file in Git.

## Interface boundary

`BOM_LOCATE_INITIAL` is a deliberately bounded P1.1 helper required to distribute the global verification input. It accepts only regular Cartesian or unrotated spherical-polar native coordinates already in the model domain. Periodic longitude normalization, reusable stage mapping, inverse mapping, and interpolation stencil construction remain P1.2.

P1-D013 resolves the P1.0 boundary ambiguity: internal owner intervals are `[west,east) x [south,north)`, so an exact internal corner belongs to the north-east tile. Multiple candidates remain fatal.

## Verification conclusion

- static syntax, ShellCheck, Python compile, fixed-form width, and `git diff --check`: pass;
- P1.1 authoritative gate `p11-state-attempt05`: 4/4 builds, 7/7 positive, 16/16 negative;
- final Phase 0 gate `p11-phase0-regression-attempt03`: pass;
- build and run evidence remained under `/home/wyl/build/mitgcm-bom` and `/home/wyl/runs/mitgcm-bom`.

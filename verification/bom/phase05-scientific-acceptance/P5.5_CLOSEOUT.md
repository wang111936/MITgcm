# MITGCM-BOM P5.5 scientific aggregate closeout

Status: **SCIENTIFIC CANDIDATE P5-SA-G99 754/754 PASS; INDEPENDENT EXIT AUDIT 21/21 PASS**

P5.5 aggregates every frozen Phase 5 scientific case and the complete released
Phase 4 predecessor gate on one clean exact candidate. This closeout records
scientific acceptance for the released regular-grid, one-way, surface scope.
It is not the later Phase 5 HPC acceptance or the v1.0 release decision.

## 1. Accepted candidate evidence

The clean exact candidate
`f3f50a77a0e01fd4a1687ead312282422999280d` on branch
`MITGCM-BOM/p5.5-scientific-g99` passed `P5-SA-G99` in
`p55-g99-f3f50a77a-attempt06`:

| Group | Expected | Actual | Result |
|---|---:|---:|---|
| P5.1 | 18 | 18 | PASS |
| P5.2 | 17 | 17 | PASS |
| P5.3 | 22 | 22 | PASS |
| P5.4 | 8 | 8 | PASS |
| Phase 4 predecessor | 689 | 689 | PASS |
| **TOTAL** | **754** | **754** | **PASS** |

Aggregate evidence root:

`/home/wyl/projects/mitgcm-bom-test-artifacts/phase05/scientific-acceptance/p5-sa-g99/p55-g99-f3f50a77a-attempt06`

The aggregate independently registered exactly 14 scientific cases: B01,
I01, J01, P01, P02, F01 spring/birth/cancel/death/coast/combined, O01, R01
and L01. Every child report used the same source head. The complete Phase 4
gate was rebuilt and rerun in an isolated shared clone rather than accepted
from historical evidence.

Key SHA-256 values:

| Evidence | SHA-256 |
|---|---|
| `expected-actual-inventory.tsv` | `07936d96c77c3a6f8564471643dde6cdf0ca9dbf7a332e8049bd9deea37e9b7c` |
| `all-rows.tsv` | `4118d2b604d3dac255a5bcaf7de303045054033208fe9f03211d120359b21338` |
| `independent-audit.log` | `0d2e91eba1b85a9c26b1b3240cc36ccac7e83fd908be31441069269caed9fe32` |
| aggregate `manifest.sha256` | `da8bef9a303495eb7f8f6fcc09f7f94fda5612d6826103662774f532f38ed336` |

The manifest rehash passed, the captured source worktree was clean before and
after the run, and the tag inventory was unchanged.

## 2. Independent scientific exit audit

The separate audit `p55-exit-f3f50a77a-attempt02` consumed the accepted
aggregate root and passed P5-D001--P5-D021 in exact order, 21/21.

Evidence root:

`/home/wyl/projects/mitgcm-bom-test-artifacts/phase05/scientific-exit-audit/p55-exit-f3f50a77a-attempt02`

Key SHA-256 values:

| Evidence | SHA-256 |
|---|---|
| `acceptance-boundary.tsv` | `a218b80cf23daa064b8938b8abe2f8a1034251e18257c1f68ea3477d5d998338` |
| `decision-trace.tsv` | `3ecf479b783594fc1795d3dd39320750e3c3755a2eaec1386ff6e83c0779ea28` |
| `independent-exit-audit.log` | `657331e728e51e10f1a201baf41127896dcc4e280625dcde4550f20a80c2bd92` |
| exit `manifest.sha256` | `b34b9fd1ec53575ea257a1453bc70db19f86997f696a364009b2567e249d4e8e` |

The authoritative boundary is:

| Decision | Result |
|---|---|
| Phase 5 scientific acceptance | PASS |
| Phase 5 HPC acceptance | NOT_EVALUATED |

## 3. Problems exposed and corrected

The preserved failed attempts distinguish test-infrastructure defects from
scientific failures:

1. attempt01 found that the P5.4 project-isolation scan matched its own test
   text. The scan was limited to executable dependency and admitted path
   evidence without weakening project isolation.
2. attempt02 found that an isolated predecessor clone could not obtain the
   exact annotated v0.4 tag object. The driver now fetches that exact object
   and still omits the v0.5 tag ref.
3. attempt03 found that an outer artifact-root override leaked into the
   nested Phase 4/3 predecessor replay. Child roots are now explicitly
   isolated.
4. attempt04 completed all 754 rows, but the aggregate auditor projected
   build and run rows onto the same three-column key. The schema now audits
   the full five-column row and permits only the three frozen P0.4 logical
   build/run pairs with exact detail order.
5. attempt05 passed 754/754, then the exit audit exposed a stale P5-D007
   inventory inherited from P5.1. P5.2 deliberately adds one forcing
   read-ahead record, so the independent final count is 302 binary checks,
   316 files, 97 endpoint records and 98 forcing records.

The final attempt rebuilt and reran every child after these corrections. No
frozen formula, fixture, tolerance, expected scientific result or production
BOM routine was changed to obtain acceptance. Every failed root remains
preserved.

## 4. Isolation and release boundary

The aggregate and exit auditors admit only the MITGCM-BOM source and declared
external build/run/artifact roots. They do not read, modify or depend on
SKRIPS source, inputs, builds, runs or artifacts. P5.5 creates no tag and this
closeout performs no GitHub push, PR or merge.

Because this closeout itself changes only documentation, the resulting
closeout commit must receive one final fresh exact-head P5-SA-G99 replay and
independent exit audit before integration. That immutable external evidence
is bound by its own `source-head.txt` and manifest; the repository record is
not edited again merely to embed the final evidence hash.

## 5. Next boundary

After the final exact-head replay and integration authorization, Phase 5 may
resume with a separate HPC acceptance freeze. It must first lock the target
server toolchain, scheduler, parallel filesystem, CPU/NUMA and binding
topology; then freeze OpenMP, changed-decomposition restart, 100,000-particle
and up-to-256-rank scaling, communication/memory behavior and the less-than-
20-percent overhead benchmark. None of those results is claimed here.

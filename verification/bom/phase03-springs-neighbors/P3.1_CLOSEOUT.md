# MITGCM-BOM P3.1 closeout

Date: 2026-08-28

Decision: **PASS -- P3.1 is ready for Draft PR review.**

P3.1 is not yet integrated into `MITGCM-BOM/development`. P3.2 must not start
until P3.1 is reviewed and merged under separate user authorization. No
`MITGCM-BOM-v0.4` tag is created at this boundary.

## Immutable functional subject

- integrated P3.0 parent: `96b38052c5444c995bc9e88078066a6ba9899ead`;
- P3.1 functional head: `3c1bc5821ea6a7515dafe5b4142140c16a6cec98`;
- branch: `MITGCM-BOM/p3.1-reference-laws`;
- identity: `WangYuLin <wang111936@outlook.com>`;
- patch: 26 files, 2706 insertions and 12 deletions;
- authoritative direct gate: 34/34 PASS;
- authoritative predecessor regression: 390/390 PASS.

## Production scope audit

Only these six production files differ from the P3.0 integration parent:

1. `pkg/bom/BOM.h`;
2. `pkg/bom/bom_check.F`;
3. `pkg/bom/bom_pair_geometry.F`;
4. `pkg/bom/bom_readparms.F`;
5. `pkg/bom/bom_spring_pair.F`;
6. `pkg/bom/bom_validate_spring_config.F`.

The accepted v0.3 production dispatcher in `bom_main.F` is unchanged. The KNN
oracle is verification-only and is not linked or called by `pkg/bom`. A live
nonzero-particle spring configuration remains fail-closed until the P3.3
synchronous ensemble path exists.

The implementation provides frozen defaults/codes, fail-before-state
configuration checks, canonical finite pair geometry, overflow-safe distance,
stateless Hooke/eBOMB velocity and transactional accumulation. It deliberately
does not provide the P3.2 cell list/graph, P3.3 ghosts/ensemble RK, P3.4
components/schema 3, or P3.5 counters/performance closure.

## Verification scope

- `P3-Z01`, `P3-C01`, `P3-K01`, `P3-D01`, `B07/P3-S01`,
  `B08/P3-S02`, `P3-N03` and `P3-N05` are represented in the 34-row direct
  gate;
- serial and MPI4 sorted direct records are bitwise identical;
- the locked Julia 200 m eBOMB value passes the frozen normalized tolerance;
- 14 invalid actual namelists fail before state initialization with stable
  failure 16;
- all Phase 2 groups pass their frozen expected counts for 390/390 total;
- both manifests validate with `sha256sum -c`, and both captured Git status
  files are empty.

Exact commands, group counts, artifact roots and hashes are recorded in
`../phase03-reference-laws/TEST_RESULTS.md`.

## Issues resolved during verification

1. GNU Fortran IEEE trapping exposed non-short-circuit logical comparisons
   around non-finite values. The checks were made sequential and transactional.
2. Extreme eBOMB logistic tails raised a benign underflow status flag. Stable
   representable-tail clamps now return the exact floating-point limit without
   overflow or underflow flags.
3. The Phase 2 exch2 build exposed the documented `SET_GRID.h` include
   prerequisite. `W2_EXCH2_SIZE.h` is now included conditionally before it.
4. The historical P2.5 scope audit correctly rejected Phase 3 paths. An
   explicit P3.1 mode now uses an exact P3.1 whitelist while leaving P2.5 as
   the default behavior.
5. The first wrapper attempt redirected child evidence away from paths frozen
   by the Phase 2 aggregator. The final wrapper unsets that override and the
   exact-head 390/390 run proves canonical evidence registration.

Only the final clean-head attempts are authoritative.

## Requirement disposition

- P3-R04 and P3-R10 are verified at P3.1.
- P3-R01, P3-R02, P3-R03, P3-R08, P3-R09, P3-R17 and P3-R18 have their
  explicitly bounded P3.1 portions satisfied; later-package evidence remains
  open and is not promoted to full requirement closure.
- All other requirements remain frozen and pending their assigned P3.2--P3.5
  packages.

## Next controlled action

Batch-push the branch once, open a Draft PR against `MITGCM-BOM/development`,
and review its exact base/head, scope and evidence links. Do not merge the PR,
start P3.2 or create a tag without new user authorization.
